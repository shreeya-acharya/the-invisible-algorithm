{
  "name": "invisible-algorithm-extension",
  "private": true,
  "scripts": {
    "build": "esbuild src/content.ts --bundle --format=iife --target=chrome120 --outfile=dist/content.js",
    "watch": "npm run build -- --watch"
  }
}
